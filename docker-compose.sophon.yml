services:
  cosmo-sophon-package:
    image: ghcr.io/cosmo-wander-ai/cosmo_edge-build-env_sophon:v1
    container_name: cosmo-sophon-package
    network_mode: host
    volumes:
      - .:/workspace
      - ./build_output:/build_output
      - cosmo-npm-cache:/root/.npm
    working_dir: /workspace
    environment:
      COSMO_MODEL_GUARD_BUILD_PROFILE: "${COSMO_MODEL_GUARD_BUILD_PROFILE:-public-runtime}"
      COSMO_MODEL_GUARD_SDK_ROOT: "${COSMO_MODEL_GUARD_SDK_ROOT:-}"
      COSMO_PACKAGE_MODELS: "${COSMO_PACKAGE_MODELS:-include}"
      NPM_CONFIG_MAXSOCKETS: "${NPM_CONFIG_MAXSOCKETS:-1}"
      NPM_CONFIG_PROGRESS: "${NPM_CONFIG_PROGRESS:-false}"
      NPM_CONFIG_FETCH_RETRIES: "${NPM_CONFIG_FETCH_RETRIES:-3}"
      NPM_CONFIG_FETCH_TIMEOUT: "${NPM_CONFIG_FETCH_TIMEOUT:-120000}"
      NPM_CONFIG_PREFER_OFFLINE: "${NPM_CONFIG_PREFER_OFFLINE:-true}"
      NPM_CONFIG_UPDATE_NOTIFIER: "${NPM_CONFIG_UPDATE_NOTIFIER:-false}"
    entrypoint:
      - /bin/bash
      - /workspace/scripts/build_sophon_package.sh
    command: []

volumes:
  cosmo-npm-cache:
